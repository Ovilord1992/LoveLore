services:
  postgres:
    image: postgres:16-alpine
    container_name: amoria-postgres
    environment:
      POSTGRES_USER: amoria
      POSTGRES_PASSWORD: amoria
      POSTGRES_DB: amoria
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
